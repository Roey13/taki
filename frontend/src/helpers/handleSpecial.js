export function handleSpecial(card) {

    switch (card.shape) {
        case 'plus':
            console.log('card.shape', card.shape);
        case '+2':
            console.log('card.shape', card.shape);
        case 'taki':
            console.log('card.shape', card.shape);
        case 'stop':
            console.log('card.shape', card.shape);
        case 'revert':
            console.log('card.shape', card.shape);
        case 'king':
            console.log('card.shape', card.shape);
        case 'changeColor':
            console.log('card.shape', card.shape);
    }
}